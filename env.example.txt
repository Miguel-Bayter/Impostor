# Configuración de variables de entorno
# Copiar este archivo como .env y configurar los valores

# Puerto del servidor (Railway asigna automáticamente, pero puede especificarse)
PORT=3000

# URL del frontend (para CORS)
# Desarrollo: http://localhost:5500
# Producción: https://tu-frontend.vercel.app
# Múltiples URLs separadas por comas: http://localhost:5500,https://tu-frontend.vercel.app
FRONTEND_URL=http://localhost:5500

# Secretos para JWT (generar en producción)
# Generar secreto seguro: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_secreto_super_seguro_aqui

# Entorno (development | production)
NODE_ENV=development

# Host (0.0.0.0 para producción, localhost para desarrollo)
HOST=0.0.0.0

# Base de datos MongoDB (opcional para Fase 1)
# MONGODB_URI=mongodb://localhost:27017/impostor
# O usar MongoDB Atlas:
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/impostor

# Redis (opcional para Fase 1)
# REDIS_URL=redis://localhost:6379

