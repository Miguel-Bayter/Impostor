# Configuración de variables de entorno
# Copiar este archivo como .env y configurar los valores

# Puerto del servidor
PORT=3000

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:5500

# Secretos para JWT (generar en producción)
JWT_SECRET=tu_secreto_super_seguro_aqui

# Base de datos MongoDB (opcional para Fase 1)
# MONGODB_URI=mongodb://localhost:27017/impostor
# O usar MongoDB Atlas:
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/impostor

# Redis (opcional para Fase 1)
# REDIS_URL=redis://localhost:6379

